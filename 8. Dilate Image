import cv2
import numpy as np

img = cv2.imread("C:/Users/AISHMA GIRI/Pictures/kitty.jpg")      
kernel = np.ones((5,5), np.uint8)

dilated = cv2.dilate(img, kernel)

cv2.imshow("Original Image", img)
cv2.imshow("Dilated Image", dilated)

cv2.waitKey(0)
cv2.destroyAllWindows()
